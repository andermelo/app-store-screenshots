{
  "name": "showapp-mcp",
  "version": "0.1.0",
  "description": "A light, local-first App Store screenshot studio for humans and MCP agents.",
  "type": "module",
  "license": "MIT",
  "author": "Anderson Melo",
  "homepage": "https://showapp.me",
  "repository": {
    "type": "git",
    "url": "https://github.com/andermelo/showapp-mcp"
  },
  "bin": {
    "showapp": "./bin/showapp.mjs"
  },
  "files": [
    "bin",
    "src",
    "web",
    "dist",
    "skills",
    "scripts",
    ".codex-plugin",
    ".claude-plugin",
    ".mcp.json",
    "README.md"
  ],
  "scripts": {
    "build": "esbuild ./bin/showapp.mjs --bundle --minify --platform=node --format=esm --target=node24 --outfile=./dist/showapp.mjs --banner:js='import { createRequire as __createRequire } from \"node:module\"; const require = __createRequire(import.meta.url);' && node ./scripts/clean-dist.mjs",
    "dev": "node --no-warnings --watch ./bin/showapp.mjs studio --no-open",
    "start": "node --no-warnings ./bin/showapp.mjs studio",
    "mcp": "node --no-warnings ./bin/showapp.mjs mcp",
    "doctor": "node --no-warnings ./bin/showapp.mjs doctor",
    "pretest": "npm run build",
    "test": "node --test"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "puppeteer-core": "^25.9.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "esbuild": "^0.25.12"
  }
}
