{
  "name": "showapp-mcp-workspace",
  "private": true,
  "version": "0.1.0",
  "description": "Local-first, browser-based App Store screenshot studio controlled through MCP.",
  "workspaces": [
    "plugins/showapp-mcp"
  ],
  "scripts": {
    "dev": "npm run dev --workspace showapp-mcp",
    "start": "npm run start --workspace showapp-mcp",
    "mcp": "npm run mcp --workspace showapp-mcp",
    "doctor": "npm run doctor --workspace showapp-mcp",
    "test": "npm test --workspace showapp-mcp"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
